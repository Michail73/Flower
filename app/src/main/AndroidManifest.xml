<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michael.flower">

    <!-- было написано viewpager, вместо flower -->
    <application
        android:name=".Michael"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Catalog"
            android:label="@string/title_activity_catalog"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Aboutflower" />
        <activity
            android:name=".MyPlants"
            android:label="@string/title_activity_my_plants"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Addition"
            android:label="@string/title_activity_addition"
            android:parentActivityName=".Catalog"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.michael.flower.Catalog" />
        </activity>
    </application>

</manifest>